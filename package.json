{
  "name": "bootstrap-sass-prototype",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "run-p watch watch:styles",
    "build": "run-s clean clean:styles build:styles copy",
    "deploy": "run-s build publish",
    "watch": "live-server src",
    "clean": "rm -rf dist",
    "watch:styles": "stylus -w src/assets/css -o src/assets/css",
    "clean:styles": "rm -rf src/assets/css",
    "build:styles": "stylus src/assets/css -c",
    "copy": "mkdir dist && rsync -avr --exclude=\"/styl\" src/ dist",
    "publish": "gh-pages -d dist"
  },
  "author": "Malte Podolski",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5",
    "stylus": "^0.54.8"
  }
}
