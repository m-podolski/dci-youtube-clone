
const conf = {

};

const dom = getNodes();

// Make the playlists slidable

function getNodes() {

  const voiceControl = document.querySelector(conf.voiceControl);

  return {
    voiceControl,
  };
}

function init() {

}

export { init };
